'use strict';

/**
 *  pristontale-pots-ex controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pristontale-pots-ex.pristontale-pots-ex');
