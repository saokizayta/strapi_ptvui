'use strict';

/**
 * pristontale-pots-ex service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pristontale-pots-ex.pristontale-pots-ex');
